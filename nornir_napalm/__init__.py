"""Napalm plugins for nornir.

Installing this package registers a ``napalm`` connection plugin with nornir, so a host
whose ``platform`` names a napalm driver can be reached without any further setup, and
provides the tasks in :obj:`nornir_napalm.plugins.tasks` that use it.
"""
